# Why Use Dubbo?

Dubbo is a popular RPC framework in the Java ecosystem because it offers:

- **High performance** – efficient network and serialization protocols.
- **Rich ecosystem** – built‑in service discovery, load balancing, fault tolerance and monitoring.
- **Extensibility** – plugin architecture via SPI.

It is widely used in microservices to connect distributed components reliably.
