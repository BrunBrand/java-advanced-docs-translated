# Find the Non‑Repeating Number

In a list where every number appears twice except one, the single number can be found by XORing all numbers together. XOR of two identical numbers is zero and zero XOR X is X.
